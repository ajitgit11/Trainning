'''Program to Print #PythonIsFun'''

text = "Python is fun"

word_list = text.split(" ")
print(word_list)
empty_list = list()

for word in word_list:
    capital = word.capitalize()
    empty_list.append(capital)

word_join = "".join(empty_list)
print(word_join)
print("#" + word_join)
